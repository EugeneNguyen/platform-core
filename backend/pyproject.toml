[project]
name = "platform-core-backend"
version = "0.2.0"
description = "platform-core backend — shared DRF conventions (error contract, pagination, filters, uuid7 utils). No models: other modules provide their own."
requires-python = ">=3.11"
dependencies = [
    "django>=5.2,<6.0",
    "djangorestframework>=3.15",
    "django-cors-headers>=4.4",
    "psycopg[binary]>=3.1",
    "python-dotenv>=1.0",
    "uuid6>=2024.1.12",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-django>=4.8",
    "ruff>=0.6",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["core_api*", "config*"]

[tool.ruff]
line-length = 100
target-version = "py311"
